# from _pyrepl.simple_interact import _get_reader
# from _pyrepl.commands import up, down, left, right

# reader = _get_reader()

# reader.commands["slam_up"] = up
# reader.commands["slam_down"] = down
# reader.commands["slam_left"] = left
# reader.commands["slam_right"] = right

# reader.bind(r"\C-k", "slam_up")
# reader.bind(r"\C-j", "slam_down")
# reader.bind(r"\C-h", "slam_left")
# reader.bind(r"\C-s", "slam_right")


# next-history
# previous-history
